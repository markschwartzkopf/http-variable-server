{
  "name": "http-variable-server",
  "version": "0.0.0",
  "description": "Serves pages and shares variables to browsers",
  "main": "index.js",
  "scripts": {
    "watch": "npm-run-all -p watch:*",
    "watch:server": "node ./node_modules/typescript/bin/tsc -p ./ --diagnostics --watch",
    "build": "npm-run-all -s build:*",
    "build:server": "node ./node_modules/typescript/bin/tsc -p ./ --diagnostics",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markschwartzkopf/http-variable-server"
  },
  "author": "Mark Schwartzkopf",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.6",
    "@types/ws": "^8.2.2",
    "jest": "^27.4.5",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^27.1.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "ws": "^8.4.0"
  }
}
